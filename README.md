# GraminRide — UI3 Hybrid Final (Localhost-ready)

Features:
- Hybrid UI: map + form booking experience (UI option 3)
- Driver app: signup + GPS sharing
- Admin panel: view and assign bookings
- Fare calculator (vehicle-wise mixed) on server
- PWA ready (manifest + service worker + install button)
- SQLite backend (no external DB required)

Run locally:
1. unzip the project
2. cd backend
3. npm install
4. npm start
5. open http://localhost:3000/user.html in browser (or mobile on same network)

Driver:
- open /driver.html, signup and Start to share GPS

Admin:
- open /admin.html to view and assign bookings
